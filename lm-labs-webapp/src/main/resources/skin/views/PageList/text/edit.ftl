<input id="${header.idHeader}" class="input error ${txtDisabled} ${txtRequired}" ${txtDisabled} type="text" size="20" name="${header.idHeader}" value="<#if line != null && line.getEntryByIdHead(header.idHeader) != null>${line.getEntryByIdHead(header.idHeader).text?html}</#if>" />