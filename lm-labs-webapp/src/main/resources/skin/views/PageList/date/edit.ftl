<input type="text" size="8" id="${header.idHeader}" class="date-pick formfield" name="${header.idHeader}" value="<#if line != null && line.getEntryByIdHead(header.idHeader) != null && line.getEntryByIdHead(header.idHeader).date != null>${line.getEntryByIdHead(header.idHeader).date.time?string('dd/MM/yyyy')}</#if>" />